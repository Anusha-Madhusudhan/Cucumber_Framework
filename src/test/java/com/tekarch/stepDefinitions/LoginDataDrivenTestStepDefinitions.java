/**
 * 
 */
package com.tekarch.stepDefinitions;



import java.io.IOException;

import com.tekarch.utils.DataProviderClass;
import com.tekarck.pages.LoginPage;

import io.cucumber.java.Before;
import io.cucumber.java.en.When;

/**
 * 
 */
public class LoginDataDrivenTestStepDefinitions extends BaseStepDefinsClass {
	
	/*
	 * String sUserName; String sPassword; String[][] userCreds;
	 * 
	 * // @When("User enters User Name and password") // public void
	 * user_enters_user_name_and_password() throws IOException { // //
	 * userCreds=DataProviderClass.getDataFromJsonFile(); // // for(String[]
	 * s:userCreds) { // sUserName=s[0]; // sPassword=s[1]; // //
	 * lp.setUserName(sUserName, driver); // lp.setPassword(sPassword, driver); // }
	 * // // }
	 * 
	 * @Before("@dataDriven") public void getUserCredsFromJsonFile() throws
	 * IOException { lp=new LoginPage(driver);
	 * userCreds=DataProviderClass.getDataFromJsonFile();
	 * System.out.println("&&&&&&&&&&&&&&&&&&&&&&&&&&&&");
	 * System.out.println(userCreds); }
	 * 
	 * 
	 * @When("User enters User Name and password {int}") public void
	 * user_enters_user_name_and_password(Integer rowCount) {
	 * 
	 * 
	 * sUserName=userCreds[rowCount][0]; sPassword=userCreds[rowCount][1];
	 * 
	 * lp.setUserName(sUserName, driver); lp.setPassword(sPassword, driver); }
	 */
	

}
